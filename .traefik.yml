displayName: CORS Support
type: middleware

iconPath: .assets/icon.jpg
import: github.com/SergioFloresG/traefik-cors-middleware

summary: "Middleware adding CORS request support"

testData:
  allow_origins: ["*"]